var nome = "Caneta";
var quantidade = 10; // Valor literal
var preco = 6.4; //JavaScript usa padrão americano, por isso utiliza-ze ponto no lugar da virgula para casas decimais em números.
let imposto = 1.5; //let é o novo var (mas tem diferença entre as duas, mais p frente vamos ver isso)
let precoFinal = preco + imposto;

console.log(nome);
console.log(quantidade);
console.log(preco); 
console.log(imposto);
console.log(precoFinal);

nome = "Caneta BIC";
console.log(nome);

// LET é o novo VAR, é mais moderno
// ambas criam variaveis